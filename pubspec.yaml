name: pokeapp
description: A Flutter application for Pokemon information
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: 5.9.0
  riverpod: 3.0.3
  freezed_annotation: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.9.0
  freezed: ^3.2.3

flutter:
  uses-material-design: true
